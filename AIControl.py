# This is the AI Control, both board setup and actual moves
import random

#AI turn based on same system as player turn
def aiturn(){
aitarget = randint(1,10), randint(1,10)
    if aitarget = full{
        #(show hit marker on aitarget)
        print("Your ship at", aitarget, "has been hit!")
    }
    else{
        #(show miss marker on coordinate)
        print("The enemy missed.")
    }
    pturn()
    }
